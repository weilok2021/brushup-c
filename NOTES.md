# Notes

## How to resume
- The `teach` skill is user-invocation only (`disable-model-invocation: true`). Start each study session by typing `/teach` in this directory.
- Renumbered 2026-09-17: two bridge lessons (6 recursion, 7 partition and merge) were inserted before the recursive sorts, so old lessons 6 to 13 are now 8 to 15 and `exercises/07-stack/` is `exercises/09-stack/`. Every mention below uses the new numbers; learning records 0006 and 0007 keep the old ones in their text. See learning record 0008.

## Preferences (from the user)
- Tooling (gcc flags, make syntax) is scaffolding, not curriculum: give it as copy-paste templates, label it as such, never quiz on it. The user asked whether it was on-goal (2026-09-05); CS107 teaches it in week 1 itself. On-goal parts of lesson 1: header/.c split (data abstraction) and reading sanitizer reports (finding pointer bugs).
- Only the most relevant C fundamentals. No full language tour. (2026-09-05)
- Scaffold: follow the boot.dev DSA course order, re-implemented in C; pull the C prerequisites from boot.dev Memory Management in C. Both trimmed against the CS107 prerequisite statement.
- Has C fundamentals but has not used them for a while. Depth unverified; calibrate with a short diagnostic in lesson 1.
- Explanations in direct sentences: subject, verb, object, one idea each. Show a worked numeric example before naming a type or a term. (2026-09-05: the lesson 2 paragraph on pointer subtraction and `ptrdiff_t` was unreadable to the user; rewritten with a diagram and short sentences.)
- **Assigned reading must be in C, or language-free theory.** The user asked for this twice (2026-09-12, lesson 5, then extended to every lesson 5-12). Rule for the Read Next list in every lesson: first choice is high-quality C code or C-based teaching; if no high-quality C material exists for a topic, use pure theory (pseudocode, invariants, hand traces) rather than Java or Python. Java (algs4) or Python may be cited inline as evidence for a claim, never assigned as reading. Reason given: mixing languages adds friction to study. Default Phase 2 primary source: Weiss's free DSAA-in-C source files (see RESOURCES.md). Any lesson issued from now on follows this rule without being asked.
- The user wants a proof, not a feeling, for index-heavy code (2026-09-18, binary search: "hard to prove the edge cases or that split in half will work 100%"). Standard tool from lesson 6b on: state the promise (invariant) in one sentence above the function, run the three checks (first call, every call, every return), and ship a checker that cross-checks against a slow obvious version on every small size. Offer this before the user asks when a lesson has a range-splitting function.
- Range convention (2026-09-19, the user's decision, restated the same day): the closed range `[low, high]` for binary search, in lesson 6 and from now on; the user did not rewrite `search_range` to half-open. Verified 2026-09-19: the closed body passes an exhaustive check over every sub-range `[lo, hi]` of the 8-element sorted array and every target 3 to 36 (1224 cases, 0 failures, ASan clean). Remaining mismatch at that time: the header comment still said `a[low..high)`. For future lessons the user prefers the closed range `[low, high]` with the count `N = high - low + 1`, because it is more intuitive to them for counting. Rules from now on: (1) every lesson that hands out a range function states both counts side by side, `hi - lo` and `high - low + 1`, with one concrete range as the check; (2) the user may write any exercise in the closed form, and the lesson accepts it, provided the header says `[low, high]`, the body follows closed arithmetic throughout, and the `size_t` guards from lesson 5 (`n == 0`, `mid == 0`) are present; (3) never mix conventions in one function, and say so when reviewing; (4) the existing lessons 7, 8 and 12 stay half-open as written, with a one-line note pointing here when the user reaches them. Done for lesson 7 on 2026-09-19 (see its entry); lessons 8 and 12 still need theirs. Learning record 0009 amended.
- When a lesson feels like it asks for two or more new mechanisms at once, the user asks for one or two short warm-up lessons before it rather than a thinner target lesson (2026-09-17, recursion and partition before the recursive sorts). Offer that split proactively when a lesson bundles mechanisms.
- **Teach algorithms the CS61B way** (2026-09-19, the user, on lesson 7: "I couldn't really understand the writing, like those pseudocode are really hard to follow"; asked for UC Berkeley CS61B materials and Josh Hug's teaching style for every DSA concept and algorithm). The recipe used in the lesson 7 rewrite and required from now on: (1) one concrete array drawn as cells with indices, never a text trace or `i=3 j=6` shorthand, never pseudocode; (2) the definition first, as a short list, then a "three valid, one invalid, find it" question with the answer folded away; (3) the mechanism as a click-through demo in the page (Next, Back, Restart, arrow keys, dataset buttons including the exercise's own calls and CS61B's array, a planted-bug toggle, an every-step list that expands for print), generated by a JS function that mirrors the C line for line and is checked in node against the C program's output before the lesson ships; (4) the pointers speak in the first person, in the voice of Hug's demo captions ("Hello, lovely 15.", "Grrr… 19.", "I dislike 17.", "Time to swap.", "Swapped."); (5) then the C with one comment per rule in the demo's words; (6) then short Q&A blocks for the gotchas ("Why does L need a guard and G not?"). CS61B textbook sections and demo slides are cited and may be assigned as language-free reading; Java code is inline-only. Sources and the chapter map are in RESOURCES.md under "Teaching model". Lessons 8 to 15 are still in the old style; see Working notes.
- Prefers pointer declarations in the `int* p` style because the pointer is visually prominent. Accept this in exercises; remind only when multiple declarators would make the syntax misleading. (2026-09-07)

## Environment
- Windows 11 host, Git Bash + PowerShell. No native gcc, clang, MSVC, make.
- WSL Ubuntu 24.04: gcc 13.3.0, GNU Make 4.3, gdb 15.1, valgrind 3.22 present (installed by user 2026-09-05). No clang/cmake; not needed.
- Repo path inside WSL: /mnt/c/Users/weilok.chia/Desktop/personal/brushup-c (original). Since 2026-09-12 the live checkout is the Linux-native clone at ~/workspace/brushup-c (/home/weilok/workspace/brushup-c); the IDE and this session both work there. Lesson setup steps use `cd ~/workspace/brushup-c`. The /mnt/c copy still exists; treat it as stale.
- Standard flags for every exercise: `gcc -std=gnu11 -Wall -Wextra -g -fsanitize=address,undefined`. gnu11 rather than c11 so POSIX functions (strdup, getline) are declared without feature macros.

## Planned lesson sequence (trimmed; revise as the zone of proximal development becomes clear)
Phase 1: C fundamentals (source: boot.dev Memory Management in C, ch 1-3, 6-7)
1. Two files, two bugs: header/.c split with include guard, const pointer params, size_t, first program written from memory, reading ASan/LSan reports. Makefile given as paste-once scaffolding, not taught.
2. Structs and pointers: `.` vs `->`, `&` and `*`, pass-by-pointer, arrays decay to pointers, pointer arithmetic, `sizeof`.
3. Stack vs heap: `malloc`/`calloc`/`realloc`/`free`, ownership rules, dangling pointer, leak, double free, and what each looks like under ASan.
4. Pointer-to-pointer and strings as data: `Node **`, arrays of pointers, `char *` keys, `strlen`/`strcmp`/`strdup`. Just enough for hashmap keys.

Phase 2: DSA rebuilt in C (source: boot.dev DSA order, ch 4, 7-10, 12, 14-15)
5. Arrays and sorting I: insertion sort, binary search, swap through pointers.
6. Recursion (bridge, added 2026-09-17): base case, shrink, trust; four shapes over `[lo, hi)`; gdb backtrace; stack-overflow signature.
6b. Proving binary search (interlude, added 2026-09-18): the promise, three checks, `lo <= mid < hi`, exhaustive small-case checker.
7. Partition and merge (bridge, added 2026-09-17): Hoare partition with checker, equal-key stops, the one guard, quicksort by hand, merge loop.
8. Sorting II (recursion): merge sort (merge copies its runs out, no scratch parameter), in-place quicksort, closed range.
9. Stack ADT: dynamic array with realloc growth, opaque struct, .h/.c, test program.
10. Singly linked list: push/insert/delete via `Node **`, iterative and recursive reverse, free.
11. Queue ADT: linked list with head and tail pointers, O(1) enqueue and dequeue.
12. Binary search tree: insert, search, min, height, in-order traversal, free. All recursive.
13. Hashmap: string keys, djb2 or FNV-1a, separate chaining, load factor and resize. Set = hashmap without values.
14. Graph: adjacency list, BFS using lesson 11's queue, recursive DFS, connected components.
Optional 15. Trie (array of 26 child pointers), only if time allows.

## Working notes
- 2026-09-05: repo initialised on `main`, C `.gitignore` added, workspace scaffolded. MISSION time budget still TBC.
- 2026-09-05 (lesson 2 session): cppreference moved from `/w/c/...` to `/c/...` (old links redirect). Its pages return 403 to the WebFetch tool; `curl -L` with a browser user agent works. Beej split-HTML filenames for the lesson 2-4 chapters: `pointers.html` (5), `arrays.html` (6), `structs.html` (8), `pointers2.html` (11), `manual-memory-allocation.html` (12), `pointers-iii-pointers-to-pointers-and-more.html` (23). Additional split filenames used by lessons 8 and 9: `functions.html` (4, passing by value), `incomplete-types.html` (35, the rule behind opaque structs).
- This session ran inside WSL directly (gcc, make, valgrind on PATH), so no `wsl -d Ubuntu` wrapper was needed; `powershell.exe Start-Process` still opens files on the Windows side.

- 2026-09-16: the user started lesson 9 (exercises/09-stack/ exists) having mistaken lesson 8 as done; exercises/08-recursive-sorts/ does not exist. Agreed order: finish 9, then do 8 before 12 (the BST needs the recursion warm-up). Do not mark lesson 8 completed or write its learning record until the exercise exists and builds.
- 2026-09-17: the user asked for one or two lessons on recursion and partition before the recursive sorts. Issued lessons 6 and 7 (below), renamed old lessons 6 to 13 to 8 to 15 with `git mv`, renamed `exercises/07-stack/` to `exercises/09-stack/`, and rewrote every lesson-number mention, link and localStorage key (`brushup-c-lesson-00NN-steps`) in lessons, reference sheets, NOTES.md and RESOURCES.md by script (numbers 6 to 13 shifted by two; 1 to 5 unchanged). Lesson 5's next link now goes to lesson 6; lesson 8's previous link, win box and steps 2 to 4 now assume lesson 7's `partition` and `merge`. Reference solutions and planted-bug builds for the two new lessons live in the session scratchpad under `lesson-06/build/` and `lesson-07/build/`.

- 2026-09-20: lesson 8 rewritten (see Lessons issued). `reference/partition-and-merge.html` and `reference/recursive-sorts.html` updated to the copy-based merge.
- 2026-09-19 (lesson 7 rewrite): lessons 9 to 15, issued 2026-09-12, are still in the old style (monospace text traces, pseudocode-like listings). Rewrite each with the CS61B recipe (Preferences) before the user reaches it, lesson 8 first: its `partition` and `merge` sections should point at lesson 7's demos instead of re-tracing, and its new demos should be the recursion tree (which range is being sorted, which frames are waiting) and the merge-sort level picture from Hug's merge sort demo. RESOURCES.md, "Teaching model", maps the CS61B chapters and demo decks for lessons 8 to 15. Do not renumber anything for this.
- 2026-09-19 (lesson 7, exploration): before writing Hoare's partition, the user proposed their own out-of-place scheme in `partition.c` comments: second array of the same size, pivot = leftmost, one scan of the rest, `<= pivot` fills from the left end and `> pivot` fills from the right end, the one slot left over is the pivot's. Judged correct (valid partition, pivot lands at `lo + count of items <= pivot after it`, proved by counting: n - 1 items fill n - 1 of n slots). Told to write it first as `partition_copy` alongside Hoare's, with a scratch parameter like `merge`. Consequence to expect: `main`'s expected thirteen lines are Hoare-specific; the copy scheme lands the first call at 4 not 3 and the six 7s at 5 not 3 (all equal keys go left, the same shape as planted bug 1). CS61B lecture 30 overview: "There are many particular strategies for partitioning. You are not expected to know any particular strategy." Lesson 7 is done only when Hoare's `partition` prints the thirteen lines.
- 2026-09-20 (lesson 7): the user's `is_partitioned` passed all 15 cases of a throwaway harness (`exercises/07-partition/test_partitioned.c`, not lesson material; 15 rows, checked against 7 broken checkers), then said they want the closed range `[low, high]` for this exercise. Harness rewritten closed (`high` = last index, one-element case `[0, 0]`). Expect the header, `partition` and `merge` in the closed form from the lesson's table: right loop `k <= high`, G starts at `high`, merge runs `[low, mid]` and `[mid+1, high]`, main labels `[0, 7] [4, 7] [0, 2] [0, 3] [0, 5]`, merge call `merge(r, 0, 3, 7, scratch)`. Seen while checking: a left loop bound of `p - 1` under `size_t` wraps at `p == 0` and shows as an ASan heap-buffer-overflow, not a wrong verdict.
- 2026-09-20 (lesson 7, exploration continued): `main.c` now has `run_scan` (the lesson's `run_partition` shape) and seven closed-range calls. The user's copy scheme printed the original array (no copy-back), a relative index on `[5, 7]`, and three LSan leaks; they asked whether the result must live in `a` (yes: contract, and lesson 8's recursion) and then asked for an in-place version of their own rules. Replaced `partition_copy` with `partition_one_scan` in partition.c/.h/main.c: one pointer i looks at each item, `<= pivot` stays and i moves on, `> pivot` is swapped to slot j at the right end and j steps in; ends with i == j + 1, pivot swaps into j. Same landing index as the copy scheme (lo + count of items <= pivot after it), so six 7s still land at 5. Builds warning-free, seven yes lines, valgrind 1 alloc 1 free. Still to do for lesson 7: Hoare's `partition` (both pointers walk, L stops on equal too) for the thirteen lines, `merge`, the closed-range main. The user has not yet written a partition from memory; `partition_one_scan` was written by the teacher at their request.
- 2026-09-20 (lesson 7, Hoare's partition): the user wrote `partition` three times. First a lockstep loop with `L_stopped`/`G_stopped` flags and strict comparisons (walked past equal keys: first call landed at 4, six 7s at 2; with the `=` added it looped forever because the swapped items were re-examined in the same pass). Second, the four-step shape with the cross check placed before the walks (pivot swapped with an unexamined 26, `partitioned: no`; ASan stack-buffer-overflow reading `a[4]` of `small` on `9 1 2 3`). Third, the correct order: walk L (`L <= hi && a[L] < pivot`), walk G (`G >= lo && a[G] > pivot`; the guard is dead at lo 0), `if (L > G) break`, swap, move both. The user chose the strict `L > G` after asking why `>=`; explained the meet case (`5 3 5 7`: `>=` lands at 2, `>` at 1; six 7s at 3 versus 2). Teacher verified the strict version exhaustively: every array of length 1 to 6 over values 1 to 3, every closed sub-range, 19956 cases, 0 failures, ASan clean (scratchpad `hoare/exhaust.c`). Left for the user: the `partition` comment in partition.h is still half-open and partition.c has none; `run_scan`'s comment names `partition_one_scan`; `merge`; the lesson's main with the five calls and the merge; the six 7s line will read 2 unless they switch to `>=`.
- 2026-09-19: `sources/pdfcmap.py` in the session scratchpad decodes Google Slides PDF exports (2-byte glyph codes via the fonts' ToUnicode maps); `sources/totext.py` strips GitBook pages fetched with curl. Both are throwaway; recreate if a later lesson needs a Hug deck read.

## Working arrangement (agreed 2026-09-05)
- Commits: the agent commits finished lesson work on `main` itself, with a plain message. The only rule is no co-author or AI attribution line. There is no "the user commits" rule (the user, 2026-09-19, after a stale note here said otherwise).
- Conversation happens in the Windows Claude Code session; the user writes and compiles code in WSL Ubuntu.
- The agent compiles and runs the user's code from this session via WSL, passing a script on stdin to avoid quoting issues:
  `wsl -d Ubuntu -- bash -s <<'EOF' ... EOF` with `cd /mnt/c/Users/weilok.chia/Desktop/personal/brushup-c` inside.
- Verified 2026-09-05: `gcc -std=c11 -Wall -Wextra -g -fsanitize=address,undefined` builds and runs from WSL against the /mnt/c path.
- Line endings: global `core.autocrlf=true` would give CRLF Makefiles in WSL ("missing separator"). Repo pinned to LF via `.gitattributes` (`* text=auto eol=lf`) and repo-local `core.autocrlf=false`.

## Conventions
- Exercises live in `exercises/NN-slug/` with `.h`, `.c`, `Makefile`. `.gitignore` whitelists sources there so binaries of any name stay untracked.
- Every lesson ships with a reference sheet in `reference/`; lessons link to it, and it is what the user revisits.
- Lesson file names say the topic, not the title: `lessons/NNNN-<topic-slug>.html` (for example `0006-recursion-basics.html`). The memorable title stays as the `<h1>`; the browser-tab `<title>` and the kicker read `Lesson N · <topic> · <title>`. Renamed to this convention on 2026-09-17 at the user's request (the old title slugs like `shrink-then-trust` could not be read at a glance in the folder listing).

## Lessons issued
- 0001 Two Files, Two Bugs (2026-09-05; trimmed same day at the user's request, tooling teaching removed, file renamed from 0001-the-build-loop.html). Exercise `exercises/01-build-loop/`: vec.h/vec.c/main.c/Makefile, sum of four heap ints = 10; two planted bugs (off-by-one, missing free). Status: COMPLETED 2026-09-05. Built and reviewed from here: warning-free, output 10, valgrind clean. Learning record 0002 written; GLOSSARY.md started.

- 0002 Structs by Pointer (issued 2026-09-05; completed 2026-09-07). `lessons/0002-structs-and-pointers.html` + `reference/structs-and-pointers.html`. Exercise `exercises/02-rects/`: three struct functions, heap array, pointer walk, pointer subtraction with `ptrdiff_t`/`%td`, and exact output `largest: 3 x 3 at (12, 10), index 2`. User reports completing the planted `sizeof` and `.`/`->` bug experiments. Final build was warning-free under ASan/UBSan and Valgrind reported no errors or leaks. Learning record 0003 written.

- 0003 One Owner, One Free (issued 2026-09-07; completed 2026-09-08). `lessons/0003-heap-ownership.html` + `reference/heap-ownership.html`. Exercise `exercises/03-owned-ints/`: zeroed allocation with `calloc`, safe temporary-pointer `realloc`, initialization of the grown region, and one final free by the owner. Final build was warning-free under ASan/UBSan, printed the expected values, and passed Valgrind with no errors or leaks. The double-free experiment exposed that successful `realloc` had already ended the original block; the user read and explained the ownership transition. Learning record 0004 written.

- 0004 A Pointer to the Slot (issued 2026-09-08). `lessons/0004-strings-and-pointer-to-pointer.html` + `reference/string-slots.html`. Exercise `exercises/04-word-slots/`: build a two-level owned `char**` array with `strdup`, find by content with `strcmp`, return a pointer to a matching slot, replace through `char**`, find longest with `strlen`, and free inner strings before the outer array. Expected output: `before: pear fig banana`, `after: pear watermelon banana`, `longest: watermelon (10)`. Planted bugs: inverted `strcmp` meaning, freeing `slot` rather than `*slot`, and using a borrowed string after freeing the owner. Status: COMPLETED 2026-09-12. Warning-free under ASan/UBSan, exact expected output, Valgrind 6 allocs, 6 frees, no errors. Planted bug experiments skipped at the user's choice. Learning record 0005 written. Phase 1 complete.

- 0005 Sort, Then Halve (issued 2026-09-12; completed 2026-09-13). `lessons/0005-insertion-sort-and-binary-search.html` + `reference/sort-search.html`. First phase 2 lesson. Exercise `exercises/05-sort-search/`: `swap_ints`, insertion sort, `is_sorted`, and binary search returning a borrowed pointer or `NULL`. The user chose a guarded closed range `[low, high]` rather than the lesson's half-open range: `size_t` indices with explicit `n == 0` and `mid == 0` checks prevent the two `- 1` expressions from wrapping. The user retained `(low + high) / 2` for now because it is clearer to them; revisit the overflow-resistant midpoint when ranges become larger. Final build was warning-free under ASan/UBSan, printed the six expected lines exactly, passed targeted empty/singleton/boundary/duplicate search tests, and passed Valgrind with no errors or leaks. The `j >= 0` insertion-sort experiment was completed; the unsafe closed-search experiment and print helper were skipped by choice. Learning record 0006 written. 2026-09-12: Read Next was rewritten at the user's request to C-only sources (Weiss sort.c and fig2_9.c, Beej 5.4, CS50 shorts as optional video); algs4 links remain only as inline evidence.

- 0006 Shrink, Then Trust (issued 2026-09-17). `lessons/0006-recursion-basics.html` + `reference/recursion.html`. Bridge lesson on recursion. Exercise `exercises/06-recursion/`: `recur.h`/`recur.c`/`main.c`, four functions over a half-open range: `sum_range` (shrink by one, base `lo == hi`, returns `long`), `print_backwards` (printf after the call), `max_range` (halve, base `hi - lo == 1`, precondition at least one element), `search_range` (recursive binary search, `[lo, mid)` or `[mid + 1, hi)`, borrowed `const int*` or NULL). Data `9 4 15 9 26 5 35 4` and its sorted form `4 4 5 9 9 15 26 35`, both as heap copies via a `copy_of` helper. Expected output: `sum: 107`, `backwards: 4 35 5 26 9 15 4 9`, `max: 35`, `max of [0, 4): 15`, `found 26 at index 6`, `found 7: no`. Valgrind: 3 allocs, 3 frees. Step 6 is a gdb session (`break sum_range if lo == 3`, `run`, `backtrace`, `up`, `quit`); the lesson shows the real backtrace (frames lo 3, 2, 1, 0 then main; 64 bytes per frame under the ASan build). Planted bugs (verified in scratchpad): `max_range(a, lo, hi)` as the left call (ASan stack-overflow, every frame at the same line); printf moved above the call in `print_backwards` (silent, prints forwards); right half `[mid, hi)` in `search_range` (search for 26 succeeds, search for 7 dies with stack-overflow: `[2, 3)` calls itself); noted, not a step: deleting `sum_range`'s base case gives gcc 13 `-Winfinite-recursion` and an ASan heap-buffer-overflow READ 0 bytes after the 32-byte region under nine frames, not a stack overflow. Read Next (C only): Aspnes CPSC 223 notes 5.3 (5.3.1, 5.3.2, 5.3.3.1), CS50x Week 3 notes #recursion, CS107 GDB Guide, CS50 shorts Recursion and Call Stacks. Status: COMPLETED 2026-09-19 (learning record 0010); `search_range` shipped in the closed form by the user's choice, planted bugs not reported.

- 0006b Say Where It Must Be (issued 2026-09-18, interlude). `lessons/0006b-binary-search-invariant.html`; reference section `reference/recursion.html#promise`. Numbered 6b, not renumbered: it sits between 6 and 7 without shifting anything (`brushup-c-lesson-0006b-steps` key; lesson 6's footer links to it). Same exercise folder as lesson 6. Teaches the invariant method for `search_range`: the promise (sorted, and if target is anywhere then it is in `[lo, hi)`), three checks (first call, every call, every return), the fact `lo <= mid < hi` for every `k = hi - lo >= 1` with a table for k = 1, 2, 3, 4, 8, the user's own draft judged line by line (closed body under a half-open header; ASan `stack-buffer-underflow` at recur.c:44), and a `check_search` that cross-checks against `linear_has` for every prefix length 0 to 8 and every target 3 to 36 (306 cases). Expected output: lesson 6's six lines plus `search check: 306 cases, 0 failures`; `main` returns `failures != 0`; valgrind 3 allocs, 3 frees. Planted bugs (verified in scratchpad `lesson-06b/build/`): base case `lo > hi` (five lines print, then ASan stack-overflow, column at the left-call line, the search for 7 loops on `[3, 3)`); base case `hi - lo <= 1` returning NULL (all six lines right, then 8 `FAIL` lines, `search check: 306 cases, 8 failures`, exit 1). Read Next: Pfenning CMU 15-122 lecture 6 PDF (C0 with contracts, half-open), Bentley 1983 CACM column PDF, Aspnes 5.3.3.1, Bloch 2006 (Java, five minutes). Wikipedia and Cornell CS 2112 as inline evidence. Learning record 0009. Status: COMPLETED 2026-09-19 with lesson 6 (learning record 0010); the `check_search` step was not reported, the teacher's exhaustive probe stood in for it.
- 0007 Scan, Stop, Swap (issued 2026-09-17). `lessons/0007-partition-and-merge.html` + `reference/partition-and-merge.html`. Bridge lesson on the partition and merge loops, no recursion. Exercise `exercises/07-partition/`: `partition.h`/`partition.c`/`main.c` with `swap_ints`, `is_sorted` (from lesson 5), `partition` (identical to lesson 8's: pivot `a[lo]`, `i < hi` guard, no `j` guard, stops on equal keys, returns `j`), `is_partitioned(a, lo, hi, p)` checker, `merge(a, lo, mid, hi, scratch)` (identical to lesson 8's; `main` owns scratch). `main` runs one level of quicksort by hand: partition `[0, 8)` (pivot 9 lands at 3), `[4, 8)` (26 at 6), `[0, 3)` (5 at 2), then `9 1 2 3` over `[0, 4)` (largest pivot, lands at 3) and six 7s over `[0, 6)` (lands at 3), then merges `4 9 9 15 | 4 5 26 35`. Expected output: 13 lines, `before: ...`, five `[lo, hi) pivot P lands at p: <array>` lines each followed by `partitioned: yes`, `merged: 4 4 5 9 9 15 26 35`, `sorted: yes`. Valgrind: 6 allocs, 6 frees. Planted bugs (verified in scratchpad): `a[i] <= pivot` in the i scan (silent; first line lands at 4, the six 7s land at 5, every checker still yes); `i < hi` guard removed (ASan heap-buffer-overflow READ of size 4, 0 bytes after the 16-byte region of the `9 1 2 3` copy, in `partition` under `run_partition`); the `while (j < hi)` tail copy deleted (ASan build prints `merged: 4 4 5 9 9 15 -1094795586 -1094795586`, `sorted: no`; valgrind "Conditional jump or move depends on uninitialised value(s)" in printf under the print helper). Read Next (C or language-free): Weiss sort.c Qsort/Median3, Aspnes 5.3.4 `merge`, Wikipedia Hoare and Lomuto partition schemes, CS50 short Merge Sort; algs4 2.3 cited inline only. 2026-09-19, when the user started it and asked whether `lo`/`hi` is closed: the lesson stays half-open, and gained a section `#closed` (also on the cheat sheet) with both counts side by side and every line that differs in the closed form: `j = high`, guard `i <= high`, checker `k <= high`, merge runs `[low, mid]` and `[mid+1, high]` with `j = mid + 1` (so the merge call is `merge(r, 0, 3, 7, scratch)`, not mid 4), `main` ranges `[0, 7] [4, 7] [0, 2] [0, 3] [0, 5]`, same landing indices, and a forward note that lesson 8's closed quicksort needs `if (p > low)` before the `[low, p - 1]` call. Closed reference solution verified in scratchpad `lesson-07-closed/`: exact output, `-Wconversion` clean, valgrind 6 allocs 6 frees, all three planted bugs give the same reports (the `<= pivot` bug also shifts the second line to `[4, 7] pivot 9 lands at 4` because the array state differs). REWRITTEN 2026-09-19 in the CS61B style at the user's request (learning record 0011); same file, exercise, expected output, planted bugs and reports. New content: the 30.1 definition as a three-line list on the lesson's array, four arrangements A to D with D invalid (`<details>` answer); Hug's six L and G rules quoted; the partition demo (`#pdemo`: six arrays, the three `main` calls, `9 1 2 3`, six 7s, CS61B's `17 15 19 32 2 26 41 17 17`; planted-bug toggle "L also loves equal items"; every-step list; 17 steps on the first array); the C annotated per rule in L and G's words; three Q&A blocks (guard, "or equal", move by one); `is_partitioned`; one level by hand as three cell rows with `<= 9`, pivot, `>= 9` tints; the merge rule in 15.4's words and the merge demo (`#mdemo`: lesson runs and CS61B's `2 15 17 32 | 17 17 19 26 41`, scratch row filling in, 10 steps); closed section kept and shortened; steps 3 and 4 now ask the user to predict every demo caption before writing; quiz reworded in L and G terms (six-word options); Read Next leads with Hug's demo and textbook sections, then Weiss, then 15.4 plus Aspnes, then Wikipedia; previous link now lesson 6b. The trace generator (`partitionTrace`, `mergeTrace`, between `TRACE-BEGIN` and `TRACE-END` in the page script) was run in node against the C output for all six arrays and both bug states: p = 3, 6, 2, 3, 3, 4; with the bug 4 and 5; both merges correct. Reference sheet gained the L and G rules and demo links. Status: STARTED 2026-09-19, closed form expected.

- 0008 Halve, Then Trust (issued 2026-09-12 as "Halve, Then Merge" by a subagent; rewritten 2026-09-20 in the CS61B style at the user's request, scratch parameter removed). `lessons/0008-merge-sort-and-quicksort.html` + `reference/recursive-sorts.html`. Closed range `[lo, hi]` throughout, matching the user's lesson 7 code. Exercise `exercises/08-recursive-sorts/`: `sort.h` with `swap_ints`, `is_sorted`, `merge_sort(a, n)`, `quick_sort(a, n)`; static workers `merge` (lesson 7's, copies its runs out and frees them), `merge_sort_range`, `partition` (lesson 7's Hoare), `quick_sort_range` with the `if (p > lo)` guard. Two click-through demos with a call-stack column (merge sort on `9 4 15 9 26 5 35 4`; quicksort on the same and on `1 5 4 9` where the pivot lands at `lo`), traces checked in node against the C (pivots 9@3, 5@2, 4@0, 26@6, 15@5). Expected output five lines; Valgrind 17 allocs, 17 frees. Planted bugs: right call `[mid, hi]` (stack overflow, column of one line number), guard deleted (`p - 1` wraps, heap-buffer-overflow 4 bytes before the region in `partition`), `free(left)` deleted (7 blocks lost: 4,4,4,4,8,8,16 bytes). Reference solution and bug builds in the session scratchpad `l8/`. Status: ISSUED, not started.

- 0009 Grow on Push (issued 2026-09-12, written by a subagent). `lessons/0009-dynamic-array-stack.html` + `reference/stack-adt.html`. First opaque-struct ADT; the template lessons 10 to 12 copy. Exercise `exercises/09-stack/`: `stack.h` with `typedef struct stack stack_t;`, `struct stack { int* items; size_t count; size_t capacity; }` only in `stack.c`, `INITIAL_CAPACITY 2`, `static stack_grow` (doubling `realloc` through a temporary pointer, owner unchanged on failure), `stack_new`, `stack_free`, `stack_push` (1, or 0 on allocation failure with the stack intact), `stack_pop` and `stack_peek` (status return, value through `int* out`, 0 on empty), `stack_count`. Test data `10 20 30 40 50`, so the block grows twice. Expected output: `count: 5`, `peek: 50`, `popped: 50 40 30 20 10`, `count: 0`, `empty: yes`. Valgrind on the plain build: 5 allocs, 5 frees (one is printf's buffer). Planted bugs (verified in scratchpad): write before grow in `stack_push` (ASan heap-buffer-overflow WRITE of size 4, "0 bytes after 8-byte region"); `free(s)` before `free(s->items)` (gcc 13 `-Wuse-after-free` warning, then ASan heap-use-after-free READ of size 8); `s->count` in `main.c` (gcc `error: invalid use of incomplete typedef 'stack_t'`). Optional plain-build experiment: force `realloc` failure with `(size_t)1 << 62` (ASan aborts on the huge request, so plain build only). Loose end: glibc also defines `stack_t` via `<signal.h>`; the exercise never includes it, but a `conflicting types for 'stack_t'` error would mean that header got pulled in. Status: COMPLETED 2026-09-16. Warning-free under ASan/UBSan, exact expected output, and Valgrind clean with 5 allocations and 5 frees. Learning record 0007 written. Read Next rewritten to C-only sources: Weiss stackar.h/stackar.c/teststka.c (fixed-capacity array stack), Beej 12.5 realloc, 17 Multifile Projects and 35 Incomplete Types, CS50x Week 5 notes (Stacks, Arrays) and the CS50 Short "Stacks", MIT 6.006 Fall 2011 Lecture 9 notes (table doubling, language-free), CS107 Lecture 7 realloc slides, style guide and testing guide; algs4 links stay only as inline evidence.

- 0010 The Slot Before the Node (issued 2026-09-12, written by a subagent). `lessons/0010-singly-linked-list.html` + `reference/linked-list.html`. Exercise `exercises/10-list/`: opaque `list_t` (`struct list { node_t* head; size_t count; }`) and `typedef struct node node_t;` with `struct node { int value; node_t* next; }`, all private to list.c. Functions: `list_new`, `list_free` (saved-`next` walk), `list_push_front`, `list_insert_sorted` and `list_remove` (both via a `node_t** link` walk from `&l->head`, no head special case; re-derives lesson 4's "address of the slot" pattern with a slot/address table and box diagrams, per learning record 0005), `list_reverse` (iterative, three pointers), `list_print`, `list_count`. Optional stretch: recursive reverse (code on the reference sheet only). Expected output: `push: 1 3 7`, `sorted: 0 1 3 5 7 9`, `count: 6`, `remove 0: yes`, `remove 5: yes`, `remove 4: no`, `after: 1 3 7 9`, `reversed: 9 7 3 1`, `count: 4`; valgrind 8 allocs, 8 frees. Planted bugs (verified in scratchpad): `list_free` reading `cur->next` after `free(cur)` (gcc 13 `-Wuse-after-free` warning, then ASan heap-use-after-free READ of size 8, "8 bytes inside of 16-byte region"); `list_remove` head special case that neither frees nor decrements (prints `count: 5`, valgrind 16 bytes definitely lost); `list_insert_sorted` writing `*link = n` with `n->next` left NULL (prints `sorted: 0 9`, valgrind 32 bytes definitely lost plus 32 indirectly). Note: lessons 11 and 12 spell the private node type `struct node` without the typedef; both lessons say so in one sentence. Status: ISSUED. Read Next rewritten to C-only sources (2026-09-12): Kirchner's write-up of Torvalds' "good taste" `list_item **` delete (TED 2016), Weiss dsaa_c2e list.h/list.c/testlist.c (dummy header node, FindPrevious, figs 3.14/3.15 DeleteList), CS50x Week 5 notes plus CS50 Shorts Singly-Linked Lists (Doug Lloyd), Beej 23.1, 35.1 and 5.4; CS107 1268 calendar Lectures 7 and 8 for placement; the algs4 1.3 link stays in the body as evidence only. Reference footer updated to the same C sources.

- 0011 Head and Tail (issued 2026-09-12, written by a subagent). `lessons/0011-linked-queue.html` + `reference/queue-adt.html`. Exercise `exercises/11-queue/`: opaque `queue_t` (`struct node { int value; struct node* next; }` and `struct queue { head, tail, count }` private to queue.c). Functions: `queue_new`, `queue_free`, `queue_enqueue` (1 ok / 0 alloc fail), `queue_dequeue(q, int* out)` (1 removed / 0 empty), `queue_peek(const, int* out)`, `queue_count`, `queue_is_empty`. Test program enqueues 10 20 30, peeks, drains, enqueues 40 50 into the emptied queue (the reset-tail case), drains again. Expected output: `peek: 10`, `count: 3`, `first: 10 20 30`, `empty: yes`, `count: 2`, `second: 40 50`, `empty: yes`. Valgrind: 7 allocs, 7 frees. Planted bugs (verified in scratchpad): dequeue of the last node without `tail = NULL` (ASan heap-use-after-free WRITE of size 8 in `queue_enqueue`, "8 bytes inside of 16-byte region", freed by `queue_dequeue`); enqueue on empty sets tail but not head (no crash; `count: 3` next to `empty: yes`; valgrind 80 bytes definitely lost). Interface is fixed for reuse by lesson 14 (BFS): int values, `int* out` dequeue. Status: ISSUED. Read Next rewritten to C-only sources: Aspnes Yale CPSC 223 LinkedLists section 4 Queues (linked queue in C), Weiss dsaa_c2e queue.h/queue.c/testque.c (circular array queue), Beej 12.1, CS50 Shorts Queues plus CS50x week 5 notes; algs4 links kept in the body as evidence only; reference footer now cites Aspnes and Weiss instead of algs4.

- 0012 Smaller Left, Larger Right (issued 2026-09-12, written by a subagent). `lessons/0012-binary-search-tree.html` + `reference/bst.html`. Exercise `exercises/12-bst/`: opaque `bst_t` (`struct node { int key; left; right; }`, `struct bst { root; count; }` private to bst.c), public functions as thin wrappers over `static` recursive helpers: `bst_new`, `bst_free` (post-order `free_nodes`), `bst_insert` (slot pattern `insert_at(struct node** slot, int key)`, 1 inserted / 0 duplicate or alloc fail), `bst_contains`, `bst_min` (borrowed `const int*` into the leftmost node, NULL if empty), `bst_height` (-1 empty, 0 single node, algs4 BST.java convention), `bst_print_inorder`, `bst_count`. Data `50 30 70 20 40 60 80 30`. Expected output: `inserted: 7 of 8`, `inorder: 20 30 40 50 60 70 80`, `count: 7`, `min: 20`, `height: 2`, `contains 60: yes`, `contains 65: no`; valgrind 9 allocs, 9 frees (8 ours plus printf's buffer). Planted bugs (verified in scratchpad): pre-order free (gcc 13 `-Wuse-after-free` warning, then ASan heap-use-after-free READ of size 8 in `free_nodes`); `insert_at(struct node* n)` writing a copy (no warning; `inserted: 8 of 8`, empty inorder, valgrind 192 bytes in 8 blocks definitely lost); the mandatory learning-record-0005 experiment, `bst_min` pointer read after `bst_free` (plain build prints `min again: 20` and exits 0; ASan heap-use-after-free READ of size 4 in main with the freed-by / allocated-by timeline shown in the lesson). No `bst_remove`; Hibbard deletion is the extension if the user asks. Next lesson (11, hashmap): reuse bst.h's ownership-contract wording for `hashmap_get`. Status: ISSUED. Read Next rewritten to C-only sources (Weiss dsaa_c2e tree.h/tree.c/testtree.c, CS50x notes Week 5 Trees plus the CS50 Recursion short, Parlante Binary Trees via the GitHub mirror, Jessica Su CS161 Lecture 8 PDF as language-free theory); reference/bst.html footer now cites Weiss tree.c, CS50x notes, cppreference free and the CS107 Valgrind guide, with algs4 kept only as inline evidence in the lesson body.

- 0013 Hash, Then Chain (issued 2026-09-12, written by a subagent). `lessons/0013-hashmap-with-chaining.html` + `reference/hashmap.html`. Exercise `exercises/13-hashmap/`: opaque `hashmap_t` (`struct node { char* key; int value; struct node* next; }`, 24 bytes, and `struct hashmap { struct node** buckets; size_t capacity; size_t count; }` private to hashmap.c), `INITIAL_CAPACITY 4`, static helpers `hash_djb2` (`unsigned long`, `hash * 33 + c` from 5381), `index_of` (`hash % capacity`), `find_in` (chain walk with `strcmp`), `hashmap_grow` (calloc a doubled array, relink every node with a saved-next walk, free only the old array; called at the end of `hashmap_put` when `count > capacity * 3 / 4`; a failed grow is ignored and the map stays correct). Public: `hashmap_new`, `hashmap_free` (key copy, node, array, struct), `hashmap_put` (overwrite in place, else `malloc` node + `strdup` key + push at chain head; 1 ok / 0 alloc fail with nothing changed), `hashmap_get` (borrowed `const int*` into the node, NULL if absent; bst.h wording: owned by the map, dies with `hashmap_remove` of that key and with `hashmap_free`; survives a resize because nodes are relinked, never moved), `hashmap_remove` (lesson 10 slot walk from `&m->buckets[i]`, no head special case, frees key then node), `hashmap_count`, `hashmap_capacity` (so main.c can show the resize). Data: keys `pear fig banana apple kiwi mango`, values `3 12 7 5 9 4`; with 4 buckets fig, apple and mango all hash to bucket 3; with 8 they go to 3, 7, 7 (mango -> apple is the one chain). Expected output: `after 3 puts: count 3, capacity 4`, `after 6 puts: count 6, capacity 8`, `get fig: 12`, `get grape: absent`, `overwrite fig: count 6`, `get fig: 20`, `remove fig: yes`, `remove grape: no`, `count: 5`. Valgrind on the plain build: 16 allocs, 16 frees (struct, 4-slot array, 8-slot array, 6 nodes, 6 key copies, printf's buffer). Planted bugs (verified in scratchpad): `int hash = 5381;` in `hash_djb2` (no warning; UBSan `runtime error: signed integer overflow: 193502395 * 33 cannot be represented in type 'int'` on the 4th character of `pear`, then all nine lines still print correctly on both builds); `n->key = key;` instead of `strdup`, with a short main that builds every key in one `char buf[16]` via `snprintf` (gcc `-Wdiscarded-qualifiers` warning; output `after 6 puts: count 3, capacity 4`, `get fig: absent`, `get mango: 4`; then ASan `attempting free on address which was not malloc()-ed` in `hashmap_free`, `located in stack of thread T0 ... 'buf'`, `SUMMARY: AddressSanitizer: bad-free`; with the full main the first bad free is of the literal `"fig"` and ASan dies with an unreadable SEGV inside its own allocator, so the lesson uses the short main); the mandatory learning-record-0005 experiment, `const int* fig = hashmap_get(m, "fig")` read after `hashmap_remove(m, "fig")` (plain build prints `fig again: -1792341989` and exits 0; ASan `heap-use-after-free`, READ of size 4 in main, `8 bytes inside of 24-byte region`, freed by `hashmap_remove`, previously allocated by `malloc` in `hashmap_put`); optional 4th, `free(m->buckets)` deleted in `hashmap_grow` (correct output; valgrind `32 bytes in 1 blocks are definitely lost`, `calloc` in `hashmap_new`, 16 allocs 15 frees). Set = map without the value member: one paragraph near the end, not in the exercise. Read Next is C-only in the `ol.reading` form (Weiss hashsep.h/.c, hashfunc.c, testhash.c; Yigit's djb2 page; cppreference free and the CS107 guide's strdup entry; CS50 short Hash Tables as optional video); algs4 3.4 and SeparateChainingHashST.java cited inline only. Loose ends: grow check runs after the insert (algs4 checks before; either is accepted, same output); `snprintf` is not in the CS107 guide, used only in experiment 2's throwaway main; `hash_djb2` has no `(unsigned char)` cast, fine for ASCII keys; `hashmap_get` returning `&n->value` through a `const hashmap_t*` compiles without a cast because `const` on the struct does not reach the nodes. Status: ISSUED.

- 0014 Mark, Then Visit (issued 2026-09-12, written by a subagent). `lessons/0014-graph-bfs-and-dfs.html` + `reference/graph.html`. Last lesson the mission requires; lesson 15 (trie) is optional. Exercise `exercises/14-graph/`: `queue.h` and `queue.c` copied unchanged from the user's `exercises/11-queue/` (Makefile compiles `main.c graph.c queue.c`), plus opaque `graph_t` (`struct edge { int to; struct edge* next; }` and `struct graph { size_t n; struct edge** adj; }` private to graph.c). Functions: `graph_new` (calloc'd array of list heads), `graph_free` (saved-next walk per list, then array, then struct), `graph_add_edge` (undirected: both nodes allocated before either is linked, front insert on `adj[u]` and `adj[v]`, so lists read in reverse insertion order; 1 ok / 0 bad vertex or alloc fail, unchanged), `graph_vertex_count`, `graph_degree`, `graph_print`, `graph_bfs` (own `marked` from calloc and own `queue_t` from `queue_new`, both freed before return; marks when enqueued; an `ok` flag stops on enqueue failure), `graph_dfs` over `static dfs_visit(g, v, marked, order, size_t* count)` (marks on entry), `graph_components` over `static label_visit(g, v, component, id)` (the label array is the mark, filled with -1 first because 0 is a real id). Test data: 8 vertices, edges `0-1, 0-2, 1-3, 2-3, 3-4, 5-6`, vertex 7 isolated. main.c prints the lists, degree of 3, BFS from 0 + count, DFS from 0 + count, BFS from 4 + count (added beyond the brief: it is the only line that exposes the directed-by-mistake bug), components + count. Expected output (17 lines): `0: 2 1`, `1: 3 0`, `2: 3 0`, `3: 4 2 1`, `4: 3`, `5: 6`, `6: 5`, `7:`, `degree 3: 3`, `bfs from 0: 0 2 1 3 4`, `visited: 5`, `dfs from 0: 0 2 3 4 1`, `visited: 5`, `bfs from 4: 4 3 2 1 0`, `visited: 5`, `components: 0 0 0 0 0 1 1 2`, `component count: 3`. Valgrind on the plain build: 32 allocs, 32 frees (14 graph blocks, 3 marked arrays, 2 queue structs, 10 queue nodes, 2 arrays in main, printf's buffer). Planted bugs (verified in scratchpad): BFS marking on dequeue (silent on the test graph: `bfs from 0: 0 2 1 3 3 4 4`, `visited: 7`, `bfs from 4: 4 3 2 1 0 0`, `visited: 6`, valgrind clean; with a seventh edge `1-2` added in main.c: ASan heap-buffer-overflow WRITE of size 4 in `graph_bfs`, "0 bytes after 32-byte region", allocated in `main`; the lesson runs the experiment both ways); `dfs_visit` marking after its loop (ASan `stack-overflow` under a column of `dfs_visit` frames; if only the mark is moved and the `order` write stays above the loop, the first report is a heap-buffer-overflow on `order[]` instead); `graph_add_edge` linking only u to v (silent, valgrind clean at 22 allocs; `degree 3: 1`, `bfs from 4: 4`, `visited: 1`; walks from 0 and components unchanged because every test edge runs from smaller to larger); optional: `queue_free` deleted in `graph_bfs` (valgrind `24 bytes in 1 blocks are definitely lost` twice, 48 bytes in 2 blocks, by `queue_new` under `graph_bfs`; 32 allocs, 30 frees). Read Next (`ol.reading` form): Skiena's C programs (graph.h, graph.c, bfs-dfs.c, connected.c, queue.c; gcc 13 builds them with implicit-int warnings; `bfs-dfs.h` is 404), Aspnes CPSC 223 notes 5.14.4 and 5.14.5.1 plus search.c, Su CS161 lectures 10 and 11 as language-free theory; algs4 4.1 and cppreference calloc cited inline only. Loose ends: the header does not spell out the partial-count case on enqueue failure; self-loops work (u twice in its own list, degree 2, algs4 convention) but are only in the reference sheet's traps table; no "Where this lands in CS107" paragraph because the author did not fetch the syllabus. Status: ISSUED.

- 0015 Twenty-Six Slots (issued 2026-09-12, written by a subagent). OPTIONAL: the required sequence ended with lesson 14; the kicker says "Optional" and the opening paragraph says so. `lessons/0015-trie.html` + `reference/trie.html`. Exercise `exercises/15-trie/`: opaque `trie_t` (`#define ALPHABET 26`, `struct node { struct node* child[ALPHABET]; int is_end; }` = 216 bytes, `struct trie { struct node* root; size_t count; }`, both private to trie.c; the root is a live node even when empty). Functions: `trie_new`, `trie_free` (post-order `free_nodes` with a loop over 26 slots, then `free(t)`), `trie_insert` (1 inserted / 0 duplicate / -1 bad char or alloc fail; the header states that a partial path may remain after -1, owned and freed by `trie_free`), `trie_contains`, `trie_starts_with` ("" always 1: the root), `trie_count`, `trie_node_size` (accessor so main.c can print `sizeof(struct node)`). Private helpers: `slot_index` (range check, then `c - 'a'`, or -1), `new_node` (`calloc`), `find_node` (shared by contains and starts_with; the difference is one `is_end` test). Lowercase 'a'..'z' only; no `<string.h>` function used. Data: `apple app apply banana band bandana app` (second `app` is the duplicate). Expected output (13 lines): `node: 216 bytes`, `inserted: 6 of 7`, `count: 6`, `contains "app": yes`, `contains "ap": no`, `contains "apple": yes`, `contains "bandana": yes`, `contains "cat": no`, `starts_with "ap": yes`, `starts_with "ban": yes`, `starts_with "c": no`, `starts_with "": yes`, `count: 6`. Valgrind on the plain build: 19 allocs, 19 frees (17 nodes, the trie struct, printf's buffer). Planted bugs (verified in scratchpad): `slot_index` returning `c` instead of `c - 'a'` (no gcc warning; UBSan `runtime error: index 97 out of bounds for type 'node *[26]'`, then ASan heap-buffer-overflow READ of size 8, "560 bytes after 216-byte region", allocated by `calloc` in `new_node` under `trie_new`; 97 x 8 - 216 = 560, so the number changes if the first word does not start with 'a'); `trie_contains` returning `n != NULL` (silent: prints `contains "ap": yes`, valgrind 0 errors); `free(n)` moved above the loop in `free_nodes` (gcc 13 `warning: pointer 'n' may be used after 'free' [-Wuse-after-free]`, "may be" rather than lesson 12's "used after"; then ASan heap-use-after-free READ of size 8, "0 bytes inside of 216-byte region" in `free_nodes`). Aside, also verified and mentioned once: `malloc` instead of `calloc` gives UBSan `member access within misaligned address 0xbebebebebebebebe for type 'struct node'` then an ASan SEGV; valgrind reports "Conditional jump or move depends on uninitialised value(s)" in `trie_insert`. Read Next is C-based only (CS50 Tries short, Doug Lloyd, video.cs50.io/MC-iQHFdEDI, verified via YouTube oEmbed; CS50x Week 5 notes Tries section; Beej 14.2 and 7.6; cppreference calloc); algs4 TrieST.java cited inline only; Weiss and Skiena have no trie source (checked). Loose ends: `trie_node_size()` exists only so main.c can print the first line, drop it and that line if a six-function interface is preferred; the reference sheet's traps table has two rows (no range check before `c - 'a'`) that name the reporting tool without a quoted message. Status: ISSUED.

## Findings
- 2026-09-05, LeakSanitizer misses leaks at -O0. User's lesson-1 program with `free` removed ran silent under `-fsanitize=address`; valgrind reported 16 bytes definitely lost. Cause verified: stale copies of the pointer in dead stack frames are picked up by LSan's exit-time stack scan, so the block counts as reachable. Reported correctly at -O1 or after scrubbing the stack. Decision: ASan stays for overflow/UAF (immediate, precise); leaks are checked with valgrind via a `make check` target on a plain build. Makefile template updated in lesson 1, reference sheet and the user's exercise folder.
- 2026-09-05, `malloc(n * sizeof ptr)` (pointer instead of pointee) is silent under `-Wall -Wextra` with gcc 13; only the run catches it, and only when the block is then overrun. Chosen as lesson 2's first planted bug for that reason. `struct rect` was given four ints (16 bytes) so the bug is visible; a two-int struct is 8 bytes, the same as a pointer, and the bug would have been silent at runtime too.
- 2026-09-12 (lesson 4 in progress): first draft of `word_replace` looped over slots with a `sizeof` length guess, freed characters one by one, and stored the borrowed `replacement` pointer. Discussed: a `char**` carries no length (count, sentinel, or contract), a heap block is freed whole by its start pointer, and "heap copy" means the slot must own a fresh block. The user said the pattern of a finder returning a slot address, instead of an index used in a later loop, is new to them and asked for time to digest. Reinforce this "return the address of the thing to change" pattern in lesson 10 (`Node **`) rather than assuming it.
- 2026-09-12 (lessons 8, 10, 11, 12 authoring): LeakSanitizer at -O0 DID report the planted leaks in these lessons (for example the missing `free(scratch)` in `merge_sort`, 32 bytes), unlike the lesson 1 leak. Difference: the only copy of the leaked pointer lived in a frame that later calls overwrote, so the exit-time stack scan found no stale copy. The lesson 1 pointer lived in `main`'s frame, which stayed intact. Conclusion unchanged: LSan at -O0 is unreliable either way; `make check` (valgrind, plain build) stays the leak gate, and the lessons say so.
- 2026-09-12 (lesson 9 authoring): forcing `realloc` failure with `SIZE_MAX` trips gcc `-Walloc-size-larger-than=` and a valgrind "silly arg" error; `(size_t)1 << 62` is below `PTRDIFF_MAX`, warning-free, and valgrind returns a clean NULL. The ASan build aborts on the request instead (default `allocator_may_return_null=0`), so the experiment is plain-build only.
- 2026-09-12 (lesson 10 authoring): when stdout is a pipe, LSan's exit path skips the stdio flush, so a leaking program's output vanishes under `./list | cat`. In a terminal all lines appear. Worth knowing if the user asks why output disappeared.
- 2026-09-12: lessons 8 to 12 were written in parallel by five subagents from a shared brief (scratchpad `BRIEF.md`). Their reference solutions and planted-bug builds live in the session scratchpad under `lesson-NN/build/` and are not in the repo. Learning records for these lessons are written only after the user completes each one.
- 2026-09-12: user asked to drop `char* const*` (pointer to const pointer) as too much to hold at once. Agreed: use `char**` for read-only array parameters in exercises for now; keep `const char*` on parameters and returns since those are already fluent. Reintroduce `char* const*` only when a cast error makes it concrete (likely hashmap or graph lessons). Do not put it in quizzes before then.
- 2026-09-12: lessons 13 to 15 were written in parallel by three subagents from a shared brief (session scratchpad `BRIEF-11-13.md`, an update of the lessons 8 to 12 brief). Their reference solutions and planted-bug builds live under `lesson-NN/build/` in that scratchpad, not in the repo. Lesson 12's "next" link now points at `0013-hashmap-with-chaining.html`. Learning records for 11 to 13 are written only after the user completes each one. A second session was rewriting the Read Next lists of lessons 8 to 12 to C-only sources at the same time; the three new lessons adopted its `ol.reading` list form, so lessons 8 to 15 all share it. Slugs: `0013-hashmap-with-chaining`, `0014-graph-bfs-and-dfs`, `0015-trie` (optional).
- 2026-09-12 (lesson 13 authoring): freeing a string literal under ASan does not give a readable report; ASan dies with a SEGV inside its own allocator. Freeing a stack buffer does give `attempting free on address which was not malloc()-ed` with the variable named. Lesson 13's borrowed-key experiment therefore builds its keys in a stack `char buf[16]` with a short throwaway main. Also: a signed `int` djb2 accumulator trips UBSan's `signed integer overflow` on the 4th character of `pear` while the program still prints every line correctly on both builds.
- 2026-09-12 (lesson 14 authoring): the mark-on-dequeue BFS bug is silent on the six-edge test graph (the duplicate visits fit inside `order[]`) and overflows only with a seventh edge; the lesson runs the experiment twice. `graph_add_edge` linking one direction only is invisible to walks from vertex 0 when every edge runs from a smaller to a larger vertex, which is why main.c has a `bfs from 4` line.
- 2026-09-18 (lesson 6b authoring): the fetch tool returns raw PDF bytes for the CMU and Tufts PDFs and no PDF tool or Python PDF library is installed in WSL (no pip either). A 20-line Python script that inflates every FlateDecode stream with zlib and collects the `Tj`/`TJ` string operands recovered the text well enough to quote (CMU's text lost its spaces; Bentley's kept them). Kept in the session scratchpad; rewrite it if needed. Also: a wrong base case on a half-open range (`lo > hi`) shows as an ASan stack-overflow, not as an out-of-bounds read, because `[x, x)` reads `a[x]` (usually in bounds) and calls itself with `[x, x)` again.
- 2026-09-17 (lessons 6 and 7 authoring): gcc 13 emits `warning: infinite recursion detected [-Winfinite-recursion]` when a recursive function has no returning path at all (base case deleted), but not when the base case exists and a call fails to shrink the range; the latter is only caught at run time as an ASan stack-overflow. ASan's malloc fills new blocks with `0xbe`, so an uninitialised int prints as `-1094795586` on the sanitizer build and as arbitrary junk on the plain build; valgrind reports the use inside printf, not at the missing write.
- 2026-09-12 (lesson 15 authoring): UBSan reports `index 97 out of bounds for type 'node *[26]'` ahead of ASan's heap-buffer-overflow only because the array bound is visible in the declaration; an index through a plain pointer gets the ASan report alone. gcc 13's `-Wuse-after-free` says "may be used after" for a use inside a loop and "used after" for a straight-line use (lesson 12).

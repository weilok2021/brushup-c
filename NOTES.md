# Notes

## How to resume
- The `teach` skill is user-invocation only (`disable-model-invocation: true`). Start each study session by typing `/teach` in this directory.

## Preferences (from the user)
- Tooling (gcc flags, make syntax) is scaffolding, not curriculum: give it as copy-paste templates, label it as such, never quiz on it. The user asked whether it was on-goal (2026-09-05); CS107 teaches it in week 1 itself. On-goal parts of lesson 1: header/.c split (data abstraction) and reading sanitizer reports (finding pointer bugs).
- Only the most relevant C fundamentals. No full language tour. (2026-09-05)
- Scaffold: follow the boot.dev DSA course order, re-implemented in C; pull the C prerequisites from boot.dev Memory Management in C. Both trimmed against the CS107 prerequisite statement.
- Has C fundamentals but has not used them for a while. Depth unverified; calibrate with a short diagnostic in lesson 1.
- Explanations in direct sentences: subject, verb, object, one idea each. Show a worked numeric example before naming a type or a term. (2026-09-05: the lesson 2 paragraph on pointer subtraction and `ptrdiff_t` was unreadable to the user; rewritten with a diagram and short sentences.)
- **Assigned reading must be in C.** Java (algs4) or Python sources create friction; the user asked for C-only Read Next lists (2026-09-12, lesson 5). Cite algs4 inline for an invariant or cost if needed, but the Read Next list is C source files, C-based course material, or Beej. Default Phase 2 primary source: Weiss's free DSAA-in-C source files (see RESOURCES.md).
- Prefers pointer declarations in the `int* p` style because the pointer is visually prominent. Accept this in exercises; remind only when multiple declarators would make the syntax misleading. (2026-09-07)

## Environment
- Windows 11 host, Git Bash + PowerShell. No native gcc, clang, MSVC, make.
- WSL Ubuntu 24.04: gcc 13.3.0, GNU Make 4.3, gdb 15.1, valgrind 3.22 present (installed by user 2026-09-05). No clang/cmake; not needed.
- Repo path inside WSL: /mnt/c/Users/weilok.chia/Desktop/personal/brushup-c (original). Since 2026-09-12 the live checkout is the Linux-native clone at ~/workspace/brushup-c (/home/weilok/workspace/brushup-c); the IDE and this session both work there. Lesson setup steps use `cd ~/workspace/brushup-c`. The /mnt/c copy still exists; treat it as stale.
- Standard flags for every exercise: `gcc -std=gnu11 -Wall -Wextra -g -fsanitize=address,undefined`. gnu11 rather than c11 so POSIX functions (strdup, getline) are declared without feature macros.

## Planned lesson sequence (trimmed; revise as the zone of proximal development becomes clear)
Phase 1: C fundamentals (source: boot.dev Memory Management in C, ch 1-3, 6-7)
1. Two files, two bugs: header/.c split with include guard, const pointer params, size_t, first program written from memory, reading ASan/LSan reports. Makefile given as paste-once scaffolding, not taught.
2. Structs and pointers: `.` vs `->`, `&` and `*`, pass-by-pointer, arrays decay to pointers, pointer arithmetic, `sizeof`.
3. Stack vs heap: `malloc`/`calloc`/`realloc`/`free`, ownership rules, dangling pointer, leak, double free, and what each looks like under ASan.
4. Pointer-to-pointer and strings as data: `Node **`, arrays of pointers, `char *` keys, `strlen`/`strcmp`/`strdup`. Just enough for hashmap keys.

Phase 2: DSA rebuilt in C (source: boot.dev DSA order, ch 4, 7-10, 12, 14-15)
5. Arrays and sorting I: insertion sort, binary search, swap through pointers.
6. Sorting II (recursion): merge sort with heap-allocated scratch, in-place quicksort.
7. Stack ADT: dynamic array with realloc growth, opaque struct, .h/.c, test program.
8. Singly linked list: push/insert/delete via `Node **`, iterative and recursive reverse, free.
9. Queue ADT: linked list with head and tail pointers, O(1) enqueue and dequeue.
10. Binary search tree: insert, search, min, height, in-order traversal, free. All recursive.
11. Hashmap: string keys, djb2 or FNV-1a, separate chaining, load factor and resize. Set = hashmap without values.
12. Graph: adjacency list, BFS using lesson 9's queue, recursive DFS, connected components.
Optional 13. Trie (array of 26 child pointers), only if time allows.

## Working notes
- 2026-09-05: repo initialised on `main`, C `.gitignore` added, workspace scaffolded. MISSION time budget still TBC.
- 2026-09-05 (lesson 2 session): cppreference moved from `/w/c/...` to `/c/...` (old links redirect). Its pages return 403 to the WebFetch tool; `curl -L` with a browser user agent works. Beej split-HTML filenames for the lesson 2-4 chapters: `pointers.html` (5), `arrays.html` (6), `structs.html` (8), `pointers2.html` (11), `manual-memory-allocation.html` (12), `pointers-iii-pointers-to-pointers-and-more.html` (23). Additional split filenames used by lessons 6 and 7: `functions.html` (4, passing by value), `incomplete-types.html` (35, the rule behind opaque structs).
- This session ran inside WSL directly (gcc, make, valgrind on PATH), so no `wsl -d Ubuntu` wrapper was needed; `powershell.exe Start-Process` still opens files on the Windows side.

## Working arrangement (agreed 2026-09-05)
- Conversation happens in the Windows Claude Code session; the user writes and compiles code in WSL Ubuntu.
- The agent compiles and runs the user's code from this session via WSL, passing a script on stdin to avoid quoting issues:
  `wsl -d Ubuntu -- bash -s <<'EOF' ... EOF` with `cd /mnt/c/Users/weilok.chia/Desktop/personal/brushup-c` inside.
- Verified 2026-09-05: `gcc -std=c11 -Wall -Wextra -g -fsanitize=address,undefined` builds and runs from WSL against the /mnt/c path.
- Line endings: global `core.autocrlf=true` would give CRLF Makefiles in WSL ("missing separator"). Repo pinned to LF via `.gitattributes` (`* text=auto eol=lf`) and repo-local `core.autocrlf=false`.

## Conventions
- Exercises live in `exercises/NN-slug/` with `.h`, `.c`, `Makefile`. `.gitignore` whitelists sources there so binaries of any name stay untracked.
- Every lesson ships with a reference sheet in `reference/`; lessons link to it, and it is what the user revisits.

## Lessons issued
- 0001 Two Files, Two Bugs (2026-09-05; trimmed same day at the user's request, tooling teaching removed, file renamed from 0001-the-build-loop.html). Exercise `exercises/01-build-loop/`: vec.h/vec.c/main.c/Makefile, sum of four heap ints = 10; two planted bugs (off-by-one, missing free). Status: COMPLETED 2026-09-05. Built and reviewed from here: warning-free, output 10, valgrind clean. Learning record 0002 written; GLOSSARY.md started.

- 0002 Structs by Pointer (issued 2026-09-05; completed 2026-09-07). `lessons/0002-structs-by-pointer.html` + `reference/structs-and-pointers.html`. Exercise `exercises/02-rects/`: three struct functions, heap array, pointer walk, pointer subtraction with `ptrdiff_t`/`%td`, and exact output `largest: 3 x 3 at (12, 10), index 2`. User reports completing the planted `sizeof` and `.`/`->` bug experiments. Final build was warning-free under ASan/UBSan and Valgrind reported no errors or leaks. Learning record 0003 written.

- 0003 One Owner, One Free (issued 2026-09-07; completed 2026-09-08). `lessons/0003-one-owner-one-free.html` + `reference/heap-ownership.html`. Exercise `exercises/03-owned-ints/`: zeroed allocation with `calloc`, safe temporary-pointer `realloc`, initialization of the grown region, and one final free by the owner. Final build was warning-free under ASan/UBSan, printed the expected values, and passed Valgrind with no errors or leaks. The double-free experiment exposed that successful `realloc` had already ended the original block; the user read and explained the ownership transition. Learning record 0004 written.

- 0004 A Pointer to the Slot (issued 2026-09-08). `lessons/0004-pointer-to-slot.html` + `reference/string-slots.html`. Exercise `exercises/04-word-slots/`: build a two-level owned `char**` array with `strdup`, find by content with `strcmp`, return a pointer to a matching slot, replace through `char**`, find longest with `strlen`, and free inner strings before the outer array. Expected output: `before: pear fig banana`, `after: pear watermelon banana`, `longest: watermelon (10)`. Planted bugs: inverted `strcmp` meaning, freeing `slot` rather than `*slot`, and using a borrowed string after freeing the owner. Status: COMPLETED 2026-09-12. Warning-free under ASan/UBSan, exact expected output, Valgrind 6 allocs, 6 frees, no errors. Planted bug experiments skipped at the user's choice. Learning record 0005 written. Phase 1 complete.

- 0005 Sort, Then Halve (issued 2026-09-12; completed 2026-09-13). `lessons/0005-sort-then-halve.html` + `reference/sort-search.html`. First phase 2 lesson. Exercise `exercises/05-sort-search/`: `swap_ints`, insertion sort, `is_sorted`, and binary search returning a borrowed pointer or `NULL`. The user chose a guarded closed range `[low, high]` rather than the lesson's half-open range: `size_t` indices with explicit `n == 0` and `mid == 0` checks prevent the two `- 1` expressions from wrapping. The user retained `(low + high) / 2` for now because it is clearer to them; revisit the overflow-resistant midpoint when ranges become larger. Final build was warning-free under ASan/UBSan, printed the six expected lines exactly, passed targeted empty/singleton/boundary/duplicate search tests, and passed Valgrind with no errors or leaks. The `j >= 0` insertion-sort experiment was completed; the unsafe closed-search experiment and print helper were skipped by choice. Learning record 0006 written. 2026-09-12: Read Next was rewritten at the user's request to C-only sources (Weiss sort.c and fig2_9.c, Beej 5.4, CS50 shorts as optional video); algs4 links remain only as inline evidence.

- 0006 Halve, Then Merge (issued 2026-09-12, written by a subagent). `lessons/0006-halve-then-merge.html` + `reference/recursive-sorts.html`. Exercise `exercises/06-recursive-sorts/`: `swap_ints` and `is_sorted` carried from lesson 5, `merge_sort` (top-level wrapper owns one heap scratch block of n ints; static `merge_sort_range(a, lo, hi, scratch)` and `merge` borrow it; half-open ranges; base case `hi - lo < 2`), `quick_sort` (static `partition` on pivot `a[lo]` returning the pivot's final index, scans stop on equal keys per algs4 2.3, static `quick_sort_range`). Data `9 4 15 9 26 5 35 4` on the heap (duplicates chosen so the equal-key stop shows in the partition trace). Expected output: `before: 9 4 15 9 26 5 35 4`, `after merge: 4 4 5 9 9 15 26 35`, `sorted: yes`, `after quick: 4 4 5 9 9 15 26 35`, `sorted: yes` (quicksort runs on a second fresh copy). Planted bugs (verified in scratchpad): copy-back loop `k <= hi` in `merge` (ASan heap-buffer-overflow READ of size 4, "0 bytes after 32-byte region", allocated in `merge_sort`); `free(scratch)` deleted in `merge_sort` (valgrind "32 bytes in 1 blocks are definitely lost", `make check` exits 1; LSan at -O0 also reported it, see Findings); base case `if (lo == hi)` in `merge_sort_range` (ASan stack-overflow). Loose ends: `merge_sort` is `void`, so a failed scratch allocation only shows as `sorted: no`; an `int` return is cleaner, raise only if the user asks. `partition`'s `j` scan has no `j > lo` guard and is safe only because the pivot is `a[lo]`. Status: ISSUED. 2026-09-12: Read Next rewritten to C-only sources (Weiss sort.c Mergesort/MSort/Merge and Quicksort/Qsort/Median3, Beej 4.1, CS107 GDB Guide, CS50 shorts Recursion/Call Stacks/Merge Sort as optional video, Beej Library Reference 24.18 qsort and CS107 lectures 7/8/14 as "later" pointers); algs4 links kept only as inline evidence. Reference sheet footer updated to match.

- 0007 Grow on Push (issued 2026-09-12, written by a subagent). `lessons/0007-grow-on-push.html` + `reference/stack-adt.html`. First opaque-struct ADT; the template lessons 8 to 10 copy. Exercise `exercises/07-stack/`: `stack.h` with `typedef struct stack stack_t;`, `struct stack { int* items; size_t count; size_t capacity; }` only in `stack.c`, `INITIAL_CAPACITY 2`, `static stack_grow` (doubling `realloc` through a temporary pointer, owner unchanged on failure), `stack_new`, `stack_free`, `stack_push` (1, or 0 on allocation failure with the stack intact), `stack_pop` and `stack_peek` (status return, value through `int* out`, 0 on empty), `stack_count`. Test data `10 20 30 40 50`, so the block grows twice. Expected output: `count: 5`, `peek: 50`, `popped: 50 40 30 20 10`, `count: 0`, `empty: yes`. Valgrind on the plain build: 5 allocs, 5 frees (one is printf's buffer). Planted bugs (verified in scratchpad): write before grow in `stack_push` (ASan heap-buffer-overflow WRITE of size 4, "0 bytes after 8-byte region"); `free(s)` before `free(s->items)` (gcc 13 `-Wuse-after-free` warning, then ASan heap-use-after-free READ of size 8); `s->count` in `main.c` (gcc `error: invalid use of incomplete typedef 'stack_t'`). Optional plain-build experiment: force `realloc` failure with `(size_t)1 << 62` (ASan aborts on the huge request, so plain build only). Loose end: glibc also defines `stack_t` via `<signal.h>`; the exercise never includes it, but a `conflicting types for 'stack_t'` error would mean that header got pulled in. Status: ISSUED.

- 0008 The Slot Before the Node (issued 2026-09-12, written by a subagent). `lessons/0008-the-slot-before-the-node.html` + `reference/linked-list.html`. Exercise `exercises/08-list/`: opaque `list_t` (`struct list { node_t* head; size_t count; }`) and `typedef struct node node_t;` with `struct node { int value; node_t* next; }`, all private to list.c. Functions: `list_new`, `list_free` (saved-`next` walk), `list_push_front`, `list_insert_sorted` and `list_remove` (both via a `node_t** link` walk from `&l->head`, no head special case; re-derives lesson 4's "address of the slot" pattern with a slot/address table and box diagrams, per learning record 0005), `list_reverse` (iterative, three pointers), `list_print`, `list_count`. Optional stretch: recursive reverse (code on the reference sheet only). Expected output: `push: 1 3 7`, `sorted: 0 1 3 5 7 9`, `count: 6`, `remove 0: yes`, `remove 5: yes`, `remove 4: no`, `after: 1 3 7 9`, `reversed: 9 7 3 1`, `count: 4`; valgrind 8 allocs, 8 frees. Planted bugs (verified in scratchpad): `list_free` reading `cur->next` after `free(cur)` (gcc 13 `-Wuse-after-free` warning, then ASan heap-use-after-free READ of size 8, "8 bytes inside of 16-byte region"); `list_remove` head special case that neither frees nor decrements (prints `count: 5`, valgrind 16 bytes definitely lost); `list_insert_sorted` writing `*link = n` with `n->next` left NULL (prints `sorted: 0 9`, valgrind 32 bytes definitely lost plus 32 indirectly). Note: lessons 9 and 10 spell the private node type `struct node` without the typedef; both lessons say so in one sentence. Status: ISSUED.

- 0009 Head and Tail (issued 2026-09-12, written by a subagent). `lessons/0009-head-and-tail.html` + `reference/queue-adt.html`. Exercise `exercises/09-queue/`: opaque `queue_t` (`struct node { int value; struct node* next; }` and `struct queue { head, tail, count }` private to queue.c). Functions: `queue_new`, `queue_free`, `queue_enqueue` (1 ok / 0 alloc fail), `queue_dequeue(q, int* out)` (1 removed / 0 empty), `queue_peek(const, int* out)`, `queue_count`, `queue_is_empty`. Test program enqueues 10 20 30, peeks, drains, enqueues 40 50 into the emptied queue (the reset-tail case), drains again. Expected output: `peek: 10`, `count: 3`, `first: 10 20 30`, `empty: yes`, `count: 2`, `second: 40 50`, `empty: yes`. Valgrind: 7 allocs, 7 frees. Planted bugs (verified in scratchpad): dequeue of the last node without `tail = NULL` (ASan heap-use-after-free WRITE of size 8 in `queue_enqueue`, "8 bytes inside of 16-byte region", freed by `queue_dequeue`); enqueue on empty sets tail but not head (no crash; `count: 3` next to `empty: yes`; valgrind 80 bytes definitely lost). Interface is fixed for reuse by lesson 12 (BFS): int values, `int* out` dequeue. Status: ISSUED.

- 0010 Smaller Left, Larger Right (issued 2026-09-12, written by a subagent). `lessons/0010-smaller-left-larger-right.html` + `reference/bst.html`. Exercise `exercises/10-bst/`: opaque `bst_t` (`struct node { int key; left; right; }`, `struct bst { root; count; }` private to bst.c), public functions as thin wrappers over `static` recursive helpers: `bst_new`, `bst_free` (post-order `free_nodes`), `bst_insert` (slot pattern `insert_at(struct node** slot, int key)`, 1 inserted / 0 duplicate or alloc fail), `bst_contains`, `bst_min` (borrowed `const int*` into the leftmost node, NULL if empty), `bst_height` (-1 empty, 0 single node, algs4 BST.java convention), `bst_print_inorder`, `bst_count`. Data `50 30 70 20 40 60 80 30`. Expected output: `inserted: 7 of 8`, `inorder: 20 30 40 50 60 70 80`, `count: 7`, `min: 20`, `height: 2`, `contains 60: yes`, `contains 65: no`; valgrind 9 allocs, 9 frees (8 ours plus printf's buffer). Planted bugs (verified in scratchpad): pre-order free (gcc 13 `-Wuse-after-free` warning, then ASan heap-use-after-free READ of size 8 in `free_nodes`); `insert_at(struct node* n)` writing a copy (no warning; `inserted: 8 of 8`, empty inorder, valgrind 192 bytes in 8 blocks definitely lost); the mandatory learning-record-0005 experiment, `bst_min` pointer read after `bst_free` (plain build prints `min again: 20` and exits 0; ASan heap-use-after-free READ of size 4 in main with the freed-by / allocated-by timeline shown in the lesson). No `bst_remove`; Hibbard deletion is the extension if the user asks. Next lesson (11, hashmap): reuse bst.h's ownership-contract wording for `hashmap_get`. Status: ISSUED.

## Findings
- 2026-09-05, LeakSanitizer misses leaks at -O0. User's lesson-1 program with `free` removed ran silent under `-fsanitize=address`; valgrind reported 16 bytes definitely lost. Cause verified: stale copies of the pointer in dead stack frames are picked up by LSan's exit-time stack scan, so the block counts as reachable. Reported correctly at -O1 or after scrubbing the stack. Decision: ASan stays for overflow/UAF (immediate, precise); leaks are checked with valgrind via a `make check` target on a plain build. Makefile template updated in lesson 1, reference sheet and the user's exercise folder.
- 2026-09-05, `malloc(n * sizeof ptr)` (pointer instead of pointee) is silent under `-Wall -Wextra` with gcc 13; only the run catches it, and only when the block is then overrun. Chosen as lesson 2's first planted bug for that reason. `struct rect` was given four ints (16 bytes) so the bug is visible; a two-int struct is 8 bytes, the same as a pointer, and the bug would have been silent at runtime too.
- 2026-09-12 (lesson 4 in progress): first draft of `word_replace` looped over slots with a `sizeof` length guess, freed characters one by one, and stored the borrowed `replacement` pointer. Discussed: a `char**` carries no length (count, sentinel, or contract), a heap block is freed whole by its start pointer, and "heap copy" means the slot must own a fresh block. The user said the pattern of a finder returning a slot address, instead of an index used in a later loop, is new to them and asked for time to digest. Reinforce this "return the address of the thing to change" pattern in lesson 8 (`Node **`) rather than assuming it.
- 2026-09-12 (lessons 6, 8, 9, 10 authoring): LeakSanitizer at -O0 DID report the planted leaks in these lessons (for example the missing `free(scratch)` in `merge_sort`, 32 bytes), unlike the lesson 1 leak. Difference: the only copy of the leaked pointer lived in a frame that later calls overwrote, so the exit-time stack scan found no stale copy. The lesson 1 pointer lived in `main`'s frame, which stayed intact. Conclusion unchanged: LSan at -O0 is unreliable either way; `make check` (valgrind, plain build) stays the leak gate, and the lessons say so.
- 2026-09-12 (lesson 7 authoring): forcing `realloc` failure with `SIZE_MAX` trips gcc `-Walloc-size-larger-than=` and a valgrind "silly arg" error; `(size_t)1 << 62` is below `PTRDIFF_MAX`, warning-free, and valgrind returns a clean NULL. The ASan build aborts on the request instead (default `allocator_may_return_null=0`), so the experiment is plain-build only.
- 2026-09-12 (lesson 8 authoring): when stdout is a pipe, LSan's exit path skips the stdio flush, so a leaking program's output vanishes under `./list | cat`. In a terminal all lines appear. Worth knowing if the user asks why output disappeared.
- 2026-09-12: lessons 6 to 10 were written in parallel by five subagents from a shared brief (scratchpad `BRIEF.md`). Their reference solutions and planted-bug builds live in the session scratchpad under `lesson-NN/build/` and are not in the repo. Learning records for these lessons are written only after the user completes each one.
- 2026-09-12: user asked to drop `char* const*` (pointer to const pointer) as too much to hold at once. Agreed: use `char**` for read-only array parameters in exercises for now; keep `const char*` on parameters and returns since those are already fluent. Reintroduce `char* const*` only when a cast error makes it concrete (likely hashmap or graph lessons). Do not put it in quizzes before then.
